import React from "react"
import { graphql } from 'gatsby'

const HomePage = () => {
  return (
    <div>
      Hello!
    </div>
  )
}

// export const query = graphql`
//   # query will go here
// `

export default HomePage
